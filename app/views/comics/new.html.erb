<%= provide(:title, "登録") %>

<h1> 漫画の追加</h1>
<h2>
<%= form_with model: Comic.new, url: {action: :create}, local: true do |f| %>
<div>
 <%= f.label :title %>
 <%= f.text_field :title, id: 'comic_title' %>
</div>

<div>
 <%= f.label :author %>
 <%= f.text_field :author, id: 'comic_author' %>
</div>

 <%= f.submit %>
<% end %>

</h2>
