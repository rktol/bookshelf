<% provide(:title, '登録ユーザ') %>
<h1>登録ユーザ</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      | <%= link_to "削除",user, method: :delete, data: {confirm: "削除してもいいですか?"} %>
    </li>
  <% end %>
</ul>
